<?xml version="1.0" encoding="utf-8"?>

        <manifest xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
            <application
                android:usesCleartextTraffic="true"
                android:allowBackup="true"
                android:dataExtractionRules="@xml/data_extraction_rules"
                android:fullBackupContent="@xml/backup_rules"
                android:icon="@drawable/financelogo"
                android:label="@string/app_name"
                android:roundIcon="@drawable/financelogo"
                android:supportsRtl="true"
                android:theme="@style/Theme.Splash"
                tools:targetApi="31">
                <activity android:name="com.example.Adhiya.BorrowerActivity" />
                <activity android:name=".ContactAddActivity" />
                <activity
                    android:name="com.example.Adhiya.MainActivity"
                    android:exported="false" />

                <activity
                    android:name="com.example.Adhiya.splashactivity"
                    android:theme="@style/Theme.Design.NoActionBar"
                    android:exported="true">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />

                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>
            </application>

        </manifest>
